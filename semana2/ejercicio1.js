const { zstdCompress } = require('zlib');
const { ask } = require('../helpers/input');
async function main () {
    let
} limite = Number (await ask ("Hasta que numero puede contar"))
let 1 = 0;
while (i <= limite ) {
    i++; 
}
i = limite
whhile (i >= 0) { "juan", "perdro", "maria", "raul"

}
const personas =["juan", "perdro", "maria", "raul"

];
console.log ("acendente");
for (let i = 0 ; i < personas.length; i++) {
    saludar (personas[i]);

}
console.log ("Descendente");
for (let i = personas.length -1; i >=0; i--){
    saludar(personas [i]);

}
console.log ("foreach");{
personas.forEach ((persona) => saludar (persona));
}
const frutas =["Fresas", "Mango", "Kiwi", "Manzana", "uvas"]
const cajon = Number (await ask ("¿Qué cajon quieres abrir?"))
for (let j = 1; j <5; j++){
    if (j=== cajon ){
       console.log ("en el cajon hay :" , frutas [j - 1]); 
    }
}

main()

