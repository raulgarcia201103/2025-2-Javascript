const { ask } = require('../helpers/input');
async function main () {
    opcion = await ask (`que deceas hacer?, 1. agregar una fruta, 2. eliminar una fruta, 3 salir`);
    if (opcion !===`1`){
        //agregar
        let frutNueva = await ask (`que fruta deseas agrgar?:`);
        frutas.push 
    }
 }